<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.afinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dnf"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dnf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Myperson"></activity>
        <activity android:name=".Mychange" />
        <activity android:name=".Dai" />
        <activity android:name=".Myrest" />
        <activity android:name=".hell" />
        <activity android:name=".Role" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>